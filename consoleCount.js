// The console.count() method logs the number of time count() function has been called.
// The count() method accepts an argument which is label to the output, it is logged every time count() function is called.
// It is optional. Its default value is "default".

for (let i = 0; i < 10; i++) {
  console.count("Count Number");
  console.log(i);
}
